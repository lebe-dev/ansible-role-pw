version: '3.3'

services:
  app:
    container_name: {{ pw_container_name }}
    image: {{ pw_image }}
    restart: always
    volumes:
      - ./pw.yml:/app/pw.yml
      - ./locale.d:/app/locale.d
    ports:
      - "{{ pw_port_mapping }}"
